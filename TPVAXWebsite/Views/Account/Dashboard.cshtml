@model TPVAXWebsite.Models.ViewModels.DashboardViewModel
@using System.Web

@{
    ViewBag.Title = "Dashboard - Tài Khoản Của Tôi";
}

@section styles {
    <style>
        .dashboard-container {
            background: #f8f9fa;
            min-height: 100vh;
            padding: 30px 0;
        }

        .sidebar-menu {
            background: white;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
            padding: 0;
            overflow: hidden;
        }

            .sidebar-menu .user-profile {
                background: linear-gradient(135deg, #0077b6 0%, #00b4d8 100%);
                color: white;
                padding: 25px;
                text-align: center;
            }

        .user-profile .avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            border: 4px solid white;
            margin: 0 auto 15px;
            background: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            color: #0077b6;
        }

        .sidebar-menu .menu-item {
            padding: 15px 25px;
            border-bottom: 1px solid #f0f0f0;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 12px;
            color: #333;
            text-decoration: none;
        }

            .sidebar-menu .menu-item:hover {
                background: #f8f9fa;
                color: #0077b6;
            }

            .sidebar-menu .menu-item.active {
                background: #e7f3ff;
                color: #0077b6;
                font-weight: 600;
                border-left: 4px solid #0077b6;
            }

            .sidebar-menu .menu-item i {
                width: 25px;
                font-size: 18px;
            }

        .dashboard-content {
            background: white;
            border-radius: 15px;
            box-shadow: 0 2px 15px rgba(0,0,0,0.08);
            padding: 30px;
        }

        .stats-card {
            background: linear-gradient(135deg, #0077b6 0%, #00b4d8 100%);
            color: white;
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 20px;
            box-shadow: 0 4px 15px rgba(0,119,182,0.2);
        }

            .stats-card .icon {
                font-size: 40px;
                opacity: 0.9;
            }

            .stats-card .number {
                font-size: 32px;
                font-weight: 700;
                margin: 10px 0 5px;
            }

        .upcoming-appointment {
            border: 2px solid #e1e8ed;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 15px;
            transition: all 0.3s;
        }

            .upcoming-appointment:hover {
                border-color: #0077b6;
                box-shadow: 0 4px 12px rgba(0,119,182,0.1);
            }

            .upcoming-appointment.border-danger {
                border-color: #dc3545 !important;
                background: linear-gradient(135deg, #fff5f5 0%, #ffffff 100%);
            }

            .upcoming-appointment.border-warning {
                border-color: #ffc107 !important;
                background: linear-gradient(135deg, #fffbeb 0%, #ffffff 100%);
            }

            .upcoming-appointment.border-danger:hover {
                box-shadow: 0 4px 12px rgba(220,53,69,0.2);
            }

            .upcoming-appointment.border-warning:hover {
                box-shadow: 0 4px 12px rgba(255,193,7,0.2);
            }

        .profile-card {
            border: 2px solid #e1e8ed;
            border-radius: 12px;
            padding: 15px;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

            .profile-card .profile-avatar {
                width: 60px;
                height: 60px;
                border-radius: 50%;
                background: #e7f3ff;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 24px;
                color: #0077b6;
            }

        .badge-status {
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        .badge-pending {
            background: #fff3cd;
            color: #856404;
        }

        .badge-completed {
            background: #d4edda;
            color: #155724;
        }

        .badge-cancelled {
            background: #f8d7da;
            color: #721c24;
        }

        .timeline-item {
            position: relative;
            padding-left: 40px;
            padding-bottom: 30px;
        }

            .timeline-item::before {
                content: "";
                position: absolute;
                left: 10px;
                top: 10px;
                bottom: -10px;
                width: 2px;
                background: #e1e8ed;
            }

            .timeline-item:last-child::before {
                display: none;
            }

            .timeline-item .timeline-icon {
                position: absolute;
                left: 0;
                top: 5px;
                width: 24px;
                height: 24px;
                border-radius: 50%;
                background: #0077b6;
                color: white;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 12px;
            }

        .quick-action-btn {
            background: white;
            border: 2px solid #0077b6;
            color: #0077b6;
            padding: 12px 24px;
            border-radius: 10px;
            font-weight: 600;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-block;
        }

            .quick-action-btn:hover {
                background: #0077b6;
                color: white;
                transform: translateY(-2px);
                box-shadow: 0 4px 12px rgba(0,119,182,0.3);
            }
    </style>
}

<!-- AntiForgeryToken for AJAX calls -->
@Html.AntiForgeryToken()

<!-- Back to Home -->
<div class="container mt-3">
    <a href="@Url.Action("Index", "Home")" class="btn btn-outline-primary">
        <i class="fas fa-arrow-left me-2"></i>Về trang chủ
    </a>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            Swal.fire({
                icon: 'success',
                title: 'Thành công!',
                text: '@Html.Raw(HttpUtility.JavaScriptStringEncode(TempData["SuccessMessage"].ToString()))',
                confirmButtonColor: '#0077b6'
            });
        });
    </script>
}

@if (TempData["ErrorMessage"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            Swal.fire({
                icon: 'error',
                title: 'Lỗi!',
                text: '@Html.Raw(HttpUtility.JavaScriptStringEncode(TempData["ErrorMessage"].ToString()))',
                confirmButtonColor: '#dc3545'
            });
        });
    </script>
}

@if (TempData["ThongBao"] != null)
{
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            Swal.fire({
                icon: 'info',
                title: 'Thông báo',
                text: '@Html.Raw(HttpUtility.JavaScriptStringEncode(TempData["ThongBao"].ToString()))',
                confirmButtonColor: '#0077b6'
            });
        });
    </script>
}

<!-- Dashboard Container -->
<div class="dashboard-container">
    <div class="container">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-lg-3 mb-4">
                <div class="sidebar-menu">
                    <!-- User Profile -->
                    <div class="user-profile">
                        <div class="avatar">
                            <i class="fas fa-user"></i>
                        </div>
                        <h5 class="mb-1">@Model.KhachHang.HoTen</h5>
                        <p class="mb-0 small opacity-75">@Model.KhachHang.Email</p>
                    </div>


                    <!-- Menu Items -->
                    <a href="#overview"
                       class="menu-item active"
                       data-section="overview">
                        <i class="fas fa-th-large"></i>
                        <span>Tổng quan</span>
                    </a>
                    <a href="#profiles" class="menu-item" data-section="profiles">
                        <i class="fas fa-users"></i>
                        <span>Hồ sơ tiêm chủng</span>
                    </a>
                    <a href="#vaccination-book"
                       class="menu-item"
                       data-section="vaccination-book">
                        <i class="fas fa-book-medical"></i>
                        <span>Sổ tiêm chủng</span>
                    </a>
                    <a href="#appointments"
                       class="menu-item"
                       data-section="appointments">
                        <i class="fas fa-calendar-check"></i>
                        <span>Lịch hẹn của tôi</span>
                    </a>
                    <a href="#invoices" class="menu-item" data-section="invoices">
                        <i class="fas fa-file-invoice-dollar"></i>
                        <span>Hóa đơn</span>
                    </a>
                    <a href="#vouchers" class="menu-item" data-section="vouchers">
                        <i class="fas fa-gift"></i>
                        <span>Voucher & Ưu đãi</span>
                    </a>
                    <a href="#account-settings"
                       class="menu-item"
                       data-section="account-settings">
                        <i class="fas fa-cog"></i>
                        <span>Cài đặt tài khoản</span>
                    </a>
                    <a href="@Url.Action("Logout", "Account")"
                       class="menu-item"
                       style="color: #dc3545">
                        <i class="fas fa-sign-out-alt"></i>
                        <span>Đăng xuất</span>
                    </a>

                </div>
            </div>

            <!-- Main Content -->
            <div class="col-lg-9">
                <!-- Overview Section -->
                <div id="section-overview"
                     class="dashboard-content section-content">
                    <h3 class="mb-4">
                        <i class="fas fa-th-large me-2 text-primary"></i>Tổng quan
                    </h3>

                    <!-- Stats Cards -->
                    <div class="row">
                        <div class="col-md-4">
                            <div class="stats-card">
                                <div class="icon"><i class="fas fa-users"></i></div>
                                <div class="number">@Model.HoSoTiemChungs.Count</div>
                                <div class="label">Hồ sơ tiêm chủng</div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="stats-card"
                                 style="background: linear-gradient(135deg, #28a745 0%, #20c997 100%)">
                                <div class="icon"><i class="fas fa-syringe"></i></div>
                                <div class="number">@Model.SoMuiHoanThanh</div>
                                <div class="label">Mũi tiêm hoàn thành</div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="stats-card"
                                 style="background: linear-gradient(135deg, #ffc107 0%, #ffab00 100%)">
                                <div class="icon"><i class="fas fa-calendar-alt"></i></div>
                                <div class="number">@Model.LichHenSapToi.Count</div>
                                <div class="label">Lịch hẹn sắp tới</div>
                            </div>
                        </div>
                    </div>

                    <!-- Upcoming Appointments -->
                    <div class="mt-4">
                        <h5 class="mb-3">
                            <i class="fas fa-calendar-check me-2 text-primary"></i>Lịch hẹn sắp tới
                        </h5>

                        @if (Model.LichHenSapToi.Any())
                        {
                            foreach (var lich in Model.LichHenSapToi.Take(3))
                            {
                                var soNgayConLai = (lich.NgayHenTiem.Date - DateTime.Now.Date).Days;
                                var soGioConLai = (lich.NgayHenTiem - DateTime.Now).TotalHours;
                                var isUrgent = soNgayConLai <= 1;
                                var isSoon = soNgayConLai <= 3 && !isUrgent;
                                var borderClass = isUrgent ? "border-danger" : (isSoon ? "border-warning" : "");
                                
                                <div class="upcoming-appointment mb-3 @borderClass">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div>
                                            <h6 class="mb-2 text-primary">
                                                <i class="fas fa-syringe me-1"></i>
                                                Vaccine @(lich.Vaccine?.TenVC ?? "Không rõ") - Mũi @(lich.SoMui ?? 0)
                                            </h6>
                                            <p class="mb-1">
                                                <i class="fas fa-user me-2"></i>Người tiêm:
                                                <strong>@(lich.HoSoTiemChung?.HoTen ?? "Không rõ")</strong>
                                            </p>
                                            <p class="mb-1">
                                                <i class="fas fa-calendar me-2"></i>Ngày:
                                                <strong>@lich.NgayHenTiem.ToString("dd/MM/yyyy")</strong> lúc 
                                                <strong>@lich.NgayHenTiem.ToString("HH:mm")</strong>
                                                @if (isUrgent)
                                                {
                                                    <span class="badge bg-danger ms-2">
                                                        <i class="fas fa-clock"></i> @(soNgayConLai == 0 ? "Hôm nay" : "Ngày mai")
                                                    </span>
                                                }
                                                else if (isSoon)
                                                {
                                                    <span class="badge bg-warning text-dark ms-2">
                                                        <i class="fas fa-clock"></i> Còn @soNgayConLai ngày
                                                    </span>
                                                }
                                            </p>
                                        </div>
                                        <span class="badge-status badge-pending">Chưa tiêm</span>
                                    </div>
                                    <div class="mt-3">
                                        @if (soNgayConLai > 0)
                                        {
                                            <button type="button" class="btn btn-primary btn-sm me-2"
                                                    onclick="openDoiLichModal('@lich.MaLT')">
                                                <i class="fas fa-edit me-1"></i>Đổi lịch
                                            </button>
                                        }
                                        else
                                        {
                                            <button type="button" class="btn btn-secondary btn-sm me-2" disabled
                                                    title="Không thể đổi lịch khi còn dưới 24h">
                                                <i class="fas fa-edit me-1"></i>Đổi lịch
                                            </button>
                                        }

                                        @if (soGioConLai > 2)
                                        {
                                            <button type="button" class="btn btn-outline-danger btn-sm"
                                                    onclick="cancelSchedule('@lich.MaLT')">
                                                <i class="fas fa-times me-1"></i>Hủy
                                            </button>
                                        }
                                        else
                                        {
                                            <button type="button" class="btn btn-outline-secondary btn-sm" disabled
                                                    title="Không thể hủy khi còn dưới 2 giờ">
                                                <i class="fas fa-times me-1"></i>Hủy
                                            </button>
                                        }
                                    </div>
                                </div>
                            }
                            
                            if (Model.LichHenSapToi.Count > 3)
                            {
                                <div class="text-center mt-3">
                                    <a href="#appointments" onclick="switchSection('appointments')" class="btn btn-outline-primary btn-sm">
                                        <i class="fas fa-list me-1"></i>Xem tất cả (@Model.LichHenSapToi.Count lịch hẹn)
                                    </a>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="text-center py-3">
                                <i class="fas fa-calendar-plus fa-2x text-muted mb-2"></i>
                                <p class="text-muted mb-2">Không có lịch hẹn nào sắp tới.</p>
                                <a href="@Url.Action("Index", "VaccinePhongBenh")" class="btn btn-primary btn-sm">
                                    <i class="fas fa-plus me-1"></i>Đặt lịch ngay
                                </a>
                            </div>
                        }
                    </div>

                    <!-- Quick Actions -->
                    <div class="mt-4">
                        <h5 class="mb-3">
                            <i class="fas fa-bolt me-2 text-primary"></i>Thao tác nhanh
                        </h5>
                        <div class="d-flex flex-wrap gap-3">
                            <a href="@Url.Action("Index", "VaccinePhongBenh")" class="quick-action-btn">
                                <i class="fas fa-plus-circle me-2"></i>Đặt lịch mới
                            </a>
                            <a href="#profiles"
                               class="quick-action-btn"
                               onclick="switchSection('profiles')">
                                <i class="fas fa-user-plus me-2"></i>Thêm hồ sơ
                            </a>
                            <a href="#vaccination-book"
                               class="quick-action-btn"
                               onclick="switchSection('vaccination-book')">
                                <i class="fas fa-download me-2"></i>Tải sổ tiêm
                            </a>
                        </div>
                    </div>
                </div>
                <!-- Profiles Section -->
                <div id="section-profiles"
                     class="dashboard-content section-content"
                     style="display: none">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h3>
                            <i class="fas fa-users me-2 text-primary"></i>Hồ sơ tiêm chủng
                        </h3>
                        <button class="btn btn-primary"
                                data-bs-toggle="modal"
                                data-bs-target="#addProfileModal">
                            <i class="fas fa-plus me-2"></i>Thêm hồ sơ người thân
                        </button>
                    </div>

                    <!-- Vai trò của chủ tài khoản -->
                    <div class="mb-3">
                        <strong>Vai trò của bạn:</strong>
                        <span class="badge bg-info">@ViewBag.VaiTroChinh</span>
                    </div>

                    <!-- Profile Cards động -->
                    @foreach (var hoSo in Model.HoSoTiemChungs)
                    {
                        var soMuiDaTiem = Model.LichHenSapToi.Count(l => l.MaHSTC == hoSo.MaHSTC && l.TrangThai == "Đã tiêm");

                        <div class="profile-card">
                            <div class="profile-avatar">
                                <i class="fas fa-user"></i>
                            </div>
                            <div class="flex-grow-1">
                                <h6 class="mb-1">@hoSo.HoTen</h6>
                                <p class="mb-0 text-muted small">
                                    <i class="fas fa-birthday-cake me-1"></i>@hoSo.NgaySinh.ToString("dd/MM/yyyy") |
                                    <i class="fas fa-venus-mars ms-2 me-1"></i>@hoSo.GioiTinh |
                                    <i class="fas fa-syringe ms-2 me-1"></i>@soMuiDaTiem mũi đã tiêm
                                </p>
                            </div>
                            <div>
                                <button class="btn btn-sm btn-outline-primary me-1"
                                        data-bs-toggle="modal"
                                        data-bs-target="#editProfileModal"
                                        onclick="loadHoSoVaoModal('@hoSo.MaHSTC', '@hoSo.HoTen', '@hoSo.GioiTinh', '@hoSo.NgaySinh.ToString("yyyy-MM-dd")', '@hoSo.CCCD', '@hoSo.GhiChu')">
                                    <i class="fas fa-edit"></i>
                                </button>
                            </div>
                        </div>
                    }
                </div>




                <!-- Vaccination Book Section -->
                <div id="section-vaccination-book"
                     class="dashboard-content section-content"
                     style="display: none">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h3>
                            <i class="fas fa-book-medical me-2 text-primary"></i>Sổ tiêm chủng điện tử
                        </h3>
                        <button class="btn btn-primary" id="btnTaiPDF" onclick="xuatPDFSoTiemChung()">
                            <i class="fas fa-download me-2"></i>Tải PDF
                        </button>
                    </div>

                    <!-- Select Profile -->
                    <div class="mb-4">
                        <label class="form-label fw-bold">Chọn hồ sơ:</label>
                        <select class="form-select" style="max-width: 300px" id="selectHoSo">
                            @foreach (var hoSo in Model.HoSoTiemChungs)
                            {
                                <option value="@hoSo.MaHSTC">@hoSo.HoTen (@hoSo.GioiTinh)</option>
                            }
                        </select>
                    </div>

                    <!-- Vaccination Timeline -->
                    <h5 class="mb-3">Lịch sử tiêm chủng</h5>

                    @foreach (var lt in Model.LichTiems)
                    {
                        <div class="timeline-item" data-hoso="@lt.MaHSTC">
                            <div class="timeline-icon">
                                <i class="fas fa-check"></i>
                            </div>
                            <div class="card border-0 shadow-sm">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-start mb-2">
                                        <h6 class="mb-0 text-primary">
                                            @(lt.Vaccine?.TenVC ?? "Không rõ vaccine") - Mũi @(lt.SoMui ?? 0)
                                        </h6>
                                        <span class="badge-status badge-completed">Đã tiêm</span>
                                    </div>
                                    <p class="mb-1 text-muted small">
                                        Ngày tiêm: @(lt.NgayTiemThucTe.HasValue
                                         ? lt.NgayTiemThucTe.Value.ToString("dd/MM/yyyy")
                                         : "Chưa tiêm")
                                    </p>
                                </div>
                            </div>
                        </div>
                    }

                    <!-- Recommended Vaccines -->
                    <div class="mt-4">
                        <h5 class="mb-3">
                            <i class="fas fa-lightbulb me-2 text-warning"></i>Vaccine đề xuất tiếp theo
                        </h5>
                        @foreach (var hen in Model.LichHenSapToi.Take(3))
                        {
                            <div class="alert alert-info">
                                <i class="fas fa-info-circle me-2"></i>
                                <strong>@(hen.Vaccine?.TenVC ?? "Không rõ vaccine"):</strong>
                                Đề xuất tiêm vào @(hen.NgayHenTiem.ToString("MM/yyyy"))
                            </div>
                        }
                    </div>
                </div> <!-- 🔴 thêm dòng này để đóng Vaccination Book -->
                <!-- Appointments Section -->
                <div id="section-appointments" class="dashboard-content section-content" style="display: none">
                    <h3 class="mb-4">
                        <i class="fas fa-calendar-check me-2 text-primary"></i>Lịch hẹn của tôi
                    </h3>

                    <!-- Tabs -->
                    <ul class="nav nav-tabs mb-4" role="tablist">
                        <li class="nav-item" role="presentation">
                            <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#upcoming-tab" type="button" role="tab">
                                Sắp tới (@Model.LichHenSapToi.Count)
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#completed-tab" type="button" role="tab">
                                Đã tiêm (@Model.LichTiems.Count)
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link" data-bs-toggle="tab" data-bs-target="#cancelled-tab" type="button" role="tab">
                                Đã hủy (@Model.LichDaHuy.Count)
                            </button>
                        </li>
                    </ul>

                    <div class="tab-content">
                        <!-- Sắp tới -->
                        <div class="tab-pane fade show active" id="upcoming-tab" role="tabpanel">
                            @if (Model.LichHenSapToi.Any())
                            {
                                foreach (var hen in Model.LichHenSapToi)
                                {
                                    var soNgayConLai = (hen.NgayHenTiem.Date - DateTime.Now.Date).Days;
                                    var isUrgent = soNgayConLai <= 1;
                                    var isSoon = soNgayConLai <= 3 && !isUrgent;
                                    var borderClass = isUrgent ? "border-danger" : (isSoon ? "border-warning" : "");
                                    
                                    <div class="upcoming-appointment mb-3 @borderClass">
                                        <div class="d-flex justify-content-between align-items-start">
                                            <div>
                                                <h6 class="mb-2 text-primary">
                                                    <i class="fas fa-syringe me-1"></i>
                                                    @(hen.Vaccine?.TenVC ?? "Không rõ vaccine") - Mũi @(hen.SoMui ?? 0)
                                                </h6>
                                                <p class="mb-1">
                                                    <i class="fas fa-user me-2"></i>Người tiêm:
                                                    <strong>@(hen.HoSoTiemChung?.HoTen ?? "Không rõ")</strong>
                                                </p>
                                                <p class="mb-1">
                                                    <i class="fas fa-calendar me-2"></i>Ngày:
                                                    <strong>@hen.NgayHenTiem.ToString("dd/MM/yyyy")</strong> lúc 
                                                    <strong>@hen.NgayHenTiem.ToString("HH:mm")</strong>
                                                    @if (isUrgent)
                                                    {
                                                        <span class="badge bg-danger ms-2">
                                                            <i class="fas fa-clock"></i> @(soNgayConLai == 0 ? "Hôm nay" : "Ngày mai")
                                                        </span>
                                                    }
                                                    else if (isSoon)
                                                    {
                                                        <span class="badge bg-warning text-dark ms-2">
                                                            <i class="fas fa-clock"></i> Còn @soNgayConLai ngày
                                                        </span>
                                                    }
                                                    else
                                                    {
                                                        <span class="badge bg-info ms-2">Còn @soNgayConLai ngày</span>
                                                    }
                                                </p>
                                                <p class="mb-0">
                                                    <i class="fas fa-map-marker-alt me-2"></i>Trung tâm TPVAX Tân Phú
                                                </p>
                                            </div>
                                            <span class="badge-status badge-pending">Chưa tiêm</span>
                                        </div>
                                        <div class="mt-3">
                                            @if (soNgayConLai > 0)
                                            {
                                                <!-- Nút đổi lịch: chỉ hiện khi còn hơn 24h -->
                                                <button type="button"
                                                        class="btn btn-primary btn-sm me-2"
                                                        onclick="openDoiLichModal('@hen.MaLT')">
                                                    <i class="fas fa-edit me-1"></i>Đổi lịch
                                                </button>
                                            }
                                            else
                                            {
                                                <button type="button" class="btn btn-secondary btn-sm me-2" disabled
                                                        title="Không thể đổi lịch khi còn dưới 24h">
                                                    <i class="fas fa-edit me-1"></i>Đổi lịch
                                                </button>
                                            }


                                            @{
                                                var soGioConLai = (hen.NgayHenTiem - DateTime.Now).TotalHours;
                                            }
                                            @if (soGioConLai > 2)
                                            {
                                                <!-- Nút hủy: chỉ hiện khi còn hơn 2h -->
                                                <button type="button"
                                                        class="btn btn-outline-danger btn-sm"
                                                        onclick="cancelSchedule('@hen.MaLT')">
                                                    <i class="fas fa-times me-1"></i>Hủy
                                                </button>
                                            }
                                            else
                                            {
                                                <button type="button" class="btn btn-outline-secondary btn-sm" disabled
                                                        title="Không thể hủy lịch khi còn dưới 2 giờ. Vui lòng liên hệ trung tâm.">
                                                    <i class="fas fa-times me-1"></i>Hủy
                                                </button>
                                            }
                                        </div>

                                    </div>
                                }
                            }
                            else
                            {
                                <div class="text-center py-4">
                                    <i class="fas fa-calendar-plus fa-3x text-muted mb-3"></i>
                                    <p class="text-muted mb-3">Không có lịch hẹn sắp tới.</p>
                                    <a href="@Url.Action("Index", "VaccinePhongBenh")" class="btn btn-primary">
                                        <i class="fas fa-plus me-1"></i>Đặt lịch tiêm ngay
                                    </a>
                                </div>
                            }
                        </div>

                        <!-- Đã tiêm -->
                        <div class="tab-pane fade" id="completed-tab" role="tabpanel">
                            @if (Model.LichTiems.Any())
                            {
                                foreach (var lt in Model.LichTiems)
                                {
                                    <div class="card mb-2">
                                        <div class="card-body">
                                            <strong>@(lt.Vaccine?.TenVC ?? "Không rõ vaccine") - Mũi @(lt.SoMui ?? 0)</strong><br />
                                            Người tiêm: @(lt.HoSoTiemChung?.HoTen ?? "Không rõ")<br />

                                            Ngày tiêm: @(lt.NgayTiemThucTe?.ToString("dd/MM/yyyy"))<br />
                                            <span class="badge bg-success">Đã tiêm</span>
                                        </div>
                                    </div>
                                }
                            }
                            else
                            {
                                <p class="text-muted">Không có lịch hẹn đã tiêm.</p>
                            }
                        </div>

                        <!-- Đã hủy -->
                        <div class="tab-pane fade" id="cancelled-tab" role="tabpanel">
                            @if (Model.LichDaHuy.Any())
                            {
                                foreach (var huy in Model.LichDaHuy)
                                {
                                    <div class="card mb-2 border-secondary">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start">
                                                <div>
                                                    <h6 class="mb-2 text-secondary">
                                                        <i class="fas fa-syringe me-1"></i>
                                                        @(huy.Vaccine?.TenVC ?? "Không rõ vaccine") - Mũi @(huy.SoMui ?? 0)
                                                    </h6>
                                                    <p class="mb-1 small">
                                                        <i class="fas fa-user me-2"></i>Người tiêm: 
                                                        <strong>@(huy.HoSoTiemChung?.HoTen ?? "Không rõ")</strong>
                                                    </p>
                                                    <p class="mb-1 small">
                                                        <i class="fas fa-calendar me-2"></i>Ngày hẹn (đã hủy): 
                                                        <span class="text-decoration-line-through">@(huy.NgayHenTiem.ToString("dd/MM/yyyy - HH:mm"))</span>
                                                    </p>
                                                    @if (!string.IsNullOrEmpty(huy.GhiChu) && huy.GhiChu.Contains("Lý do:"))
                                                    {
                                                        <p class="mb-0 small text-muted">
                                                            <i class="fas fa-comment me-2"></i>@huy.GhiChu
                                                        </p>
                                                    }
                                                </div>
                                                <span class="badge bg-secondary">Đã hủy</span>
                                            </div>
                                            <div class="mt-2">
                                                <a href="@Url.Action("Index", "VaccinePhongBenh")" class="btn btn-outline-primary btn-sm">
                                                    <i class="fas fa-redo me-1"></i>Đặt lịch lại
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                }
                            }
                            else
                            {
                                <div class="text-center py-4">
                                    <i class="fas fa-calendar-check fa-3x text-success mb-3"></i>
                                    <p class="text-muted">Không có lịch hẹn đã hủy. Tuyệt vời!</p>
                                </div>
                            }
                        </div>
                    </div>
                </div>


                <!-- Invoices Section -->
                <div id="section-invoices"
                     class="dashboard-content section-content"
                     style="display: none">
                    <h3 class="mb-4">
                        <i class="fas fa-file-invoice-dollar me-2 text-primary"></i>Hóa
                        đơn của tôi
                    </h3>

                    <div class="table-responsive">
                        <table class="table table-hover">
                            <thead class="table-light">
                                <tr>
                                    <th>Mã HĐ</th>
                                    <th>Ngày lập</th>
                                    <th>Nội dung</th>
                                    <th>Tổng tiền</th>
                                    <th>Trạng thái</th>
                                    <th>Thao tác</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model.HoaDons != null && Model.HoaDons.Any())
                                {
                                    foreach (var hd in Model.HoaDons)
                                    {
                                        <tr>
                                            <td><strong>@hd.MaHD</strong></td>
                                            <td>@hd.NgayLap.ToString("dd/MM/yyyy")</td>
                                            <td>
                                                @{
                                                    if (hd.ChiTietHoaDon != null && hd.ChiTietHoaDon.Any())
                                                    {
                                                        var firstItem = hd.ChiTietHoaDon.First();
                                                        @(firstItem.LoaiSanPham == "VACCINE" ? "Vaccine" : "Gói vaccine")
                                                        if (hd.ChiTietHoaDon.Count > 1)
                                                        {
                                                            <text> và @(hd.ChiTietHoaDon.Count - 1) sản phẩm khác</text>
                                                        }
                                                    }
                                                    else
                                                    {
                                                        <text>Chi tiết hóa đơn</text>
                                                    }
                                                }
                                            </td>
                                            <td><strong class="@(hd.TrangThai == true ? "text-primary" : "text-danger")">@hd.TongTien.ToString("N0")đ</strong></td>
                                            <td>
                                                @if (hd.TrangThai == true)
                                                {
                                                    <span class="badge-status badge-completed">Đã thanh toán</span>
                                                }
                                                else
                                                {
                                                    <span class="badge-status badge-pending">Chưa thanh toán</span>
                                                }
                                            </td>
                                            <td>
                                                @if (hd.TrangThai == true)
                                                {
                                                    <a href="@Url.Action("ChiTietHoaDon", "Account", new { maHD = hd.MaHD })" class="btn btn-sm btn-outline-primary">
                                                        <i class="fas fa-eye"></i>
                                                    </a>
                                                }
                                                else
                                                {
                                                    <button class="btn btn-sm btn-primary">
                                                        <i class="fas fa-credit-card me-1"></i>Thanh toán
                                                    </button>
                                                }
                                            </td>
                                        </tr>
                                    }
                                }
                                else
                                {
                                    <tr>
                                        <td colspan="6" class="text-center text-muted py-4">
                                            <i class="fas fa-inbox fa-3x mb-3 d-block"></i>
                                            Chưa có hóa đơn nào
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- Vouchers Section -->
                <div id="section-vouchers"
                     class="dashboard-content section-content"
                     style="display: none">
                    <h3 class="mb-4">
                        <i class="fas fa-gift me-2 text-primary"></i>Voucher & Ưu đãi
                    </h3>

                    <div class="row">
                        @if (Model.KhuyenMais != null && Model.KhuyenMais.Any())
                        {
                            foreach (var km in Model.KhuyenMais)
                            {
                                var isExpiringSoon = (km.NgayKetThuc - DateTime.Now).TotalDays <= 7;
                                var borderColor = isExpiringSoon ? "border-warning" : "border-primary";
                                var textColor = isExpiringSoon ? "text-warning" : "text-primary";
                                var btnColor = isExpiringSoon ? "btn-warning" : "btn-primary";

                                <div class="col-md-6 mb-3">
                                    <div class="card @borderColor">
                                        <div class="card-body">
                                            <div class="d-flex justify-content-between align-items-start">
                                                <div>
                                                    <h5 class="@textColor mb-2">
                                                        @if (km.KieuGiam == "PhanTram")
                                                        {
                                                            <text>Giảm @km.GiaTriGiam%</text>
                                                        }
                                                        else
                                                        {
                                                            <text>Giảm @km.GiaTriGiam.ToString("N0")đ</text>
                                                        }
                                                    </h5>
                                                    <p class="mb-2 small">@km.TenKM</p>
                                                    <p class="mb-0 text-muted small">
                                                        <i class="fas fa-clock me-1"></i>HSD: @km.NgayKetThuc.ToString("dd/MM/yyyy")
                                                    </p>
                                                    @if (isExpiringSoon)
                                                    {
                                                        <span class="badge bg-warning text-dark mt-2">
                                                            <i class="fas fa-exclamation-triangle"></i> Sắp hết hạn
                                                        </span>
                                                    }
                                                </div>
                                                <span class="badge bg-success">Có thể dùng</span>
                                            </div>
                                            <button class="btn @btnColor btn-sm mt-3 w-100" onclick="copyVoucherCode('@km.MaKM')">
                                                <i class="fas fa-copy me-1"></i>Sao chép mã: @km.MaKM
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            }
                        }
                        else
                        {
                            <div class="col-12">
                                <div class="alert alert-info">
                                    <i class="fas fa-info-circle me-2"></i>
                                    Hiện tại không có khuyến mãi nào. Vui lòng quay lại sau!
                                </div>
                            </div>
                        }
                    </div>

                    <!-- Loyalty Points -->
                    <div class="mt-4">
                        <h5 class="mb-3">
                            <i class="fas fa-star me-2 text-warning"></i>Điểm tích lũy
                        </h5>
                        <div class="card bg-light">
                            <div class="card-body">
                                <div class="row align-items-center">
                                    <div class="col-md-6">
                                        <h3 class="text-primary mb-0">1,250 điểm</h3>
                                        <p class="mb-0 text-muted">Tương đương 125.000đ</p>
                                    </div>
                                    <div class="col-md-6 text-end">
                                        <button class="btn btn-primary">
                                            <i class="fas fa-exchange-alt me-2"></i>Đổi điểm
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Account Settings Section -->
                <div id="section-account-settings"
                     class="dashboard-content section-content"
                     style="display: none">
                    <h3 class="mb-4">
                        <i class="fas fa-cog me-2 text-primary"></i>Cài đặt tài khoản
                    </h3>

                    <!-- Personal Info -->
                    <div class="card mb-3">
                        <div class="card-header bg-light">
                            <h6 class="mb-0">
                                <i class="fas fa-user me-2"></i>Thông tin cá nhân
                            </h6>
                        </div>
                        <div class="card-body">
                            @using (Html.BeginForm("CapNhatThongTin", "Account", FormMethod.Post))
                            {
                                @Html.AntiForgeryToken()
                                <div class="row">
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Họ và tên</label>
                                        <input type="text" name="HoTen" class="form-control"
                                               value="@Model.KhachHang.HoTen" required />
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Email</label>
                                        <input type="email" name="Email" class="form-control"
                                               value="@Model.KhachHang.Email" required />
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Số điện thoại</label>
                                        <input type="tel" name="SoDT" class="form-control"
                                               value="@Model.KhachHang.SoDT" />
                                    </div>
                                    <div class="col-md-6 mb-3">
                                        <label class="form-label">Ngày sinh</label>
                                        <input type="date" name="NgaySinh" class="form-control"
                                               value="@Model.KhachHang.NgaySinh.Value.ToString("yyyy-MM-dd")" />
                                    </div>
                                    <div class="col-12">
                                        <label class="form-label">Địa chỉ</label>
                                        <textarea name="DiaChi" class="form-control" rows="2">@Model.KhachHang.DiaChi</textarea>
                                    </div>
                                </div>
                                <button type="submit" class="btn btn-primary mt-3">
                                    <i class="fas fa-save me-2"></i>Lưu thay đổi
                                </button>
                            }
                        </div>
                    </div>

                    <!-- Change Password -->
                    <div class="card">
                        <div class="card-header bg-light">
                            <h6 class="mb-0">
                                <i class="fas fa-lock me-2"></i>Đổi mật khẩu
                            </h6>
                        </div>
                        <div class="card-body">
                            @using (Html.BeginForm("DoiMatKhau", "Account", FormMethod.Post))
                            {
                                @Html.AntiForgeryToken()
                                <div class="mb-3">
                                    <label class="form-label">Mật khẩu hiện tại</label>
                                    <input type="password" name="MatKhauCu" class="form-control" required />
                                </div>
                                <div class="mb-3">
                                    <label class="form-label">Mật khẩu mới</label>
                                    <input type="password" name="MatKhauMoi" class="form-control" required />
                                </div>
                                <div class="mb-3">
                                    <label class="form-label">Xác nhận mật khẩu mới</label>
                                    <input type="password" name="XacNhanMatKhauMoi" class="form-control" required />
                                </div>
                                <button type="submit" class="btn btn-primary">
                                    <i class="fas fa-key me-2"></i>Đổi mật khẩu
                                </button>
                            }
                        </div>
                    </div>
                </div>


                <!-- Add Profile Modal -->

                <div class="modal fade" id="addProfileModal" tabindex="-1">
                    <div class="modal-dialog modal-lg">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title">
                                    <i class="fas fa-user-plus me-2"></i>Thêm hồ sơ tiêm chủng mới
                                </h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                            </div>

                            <form method="post" action="@Url.Action("ThemHoSo", "HoSo")" id="formThemHoSo">
                                @Html.AntiForgeryToken()

                                <div class="modal-body">
                                    <div class="row">
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label">Họ và tên <span class="text-danger">*</span></label>
                                            <input type="text" name="HoTen" class="form-control" required />
                                        </div>
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label">Giới tính <span class="text-danger">*</span></label>
                                            <select name="GioiTinh" class="form-select" required>
                                                <option value="">Chọn giới tính</option>
                                                <option>Nam</option>
                                                <option>Nữ</option>
                                            </select>
                                        </div>
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label">Ngày sinh <span class="text-danger">*</span></label>
                                            <input type="date" name="NgaySinh" class="form-control" required />
                                        </div>
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label">CCCD/CMND <span class="text-muted">(Không bắt buộc với con)</span></label>
                                            <input type="text" name="CCCD" class="form-control" minlength="9" maxlength="12" placeholder="Nhập CCCD/CMND nếu có" />
                                        </div>
                                        <div class="col-md-12 mb-3">
                                            <label class="form-label">Quan hệ <span class="text-danger">*</span></label>
                                            <select name="VaiTro" class="form-select" required>
                                                <option value="">Chọn quan hệ</option>
                                                <option>Con</option>
                                                <option>Vợ/Chồng</option>
                                                <option>Bố/Mẹ</option>
                                                <option>Anh/Chị/Em</option>
                                                <option>Người thân khác</option>
                                            </select>
                                        </div>
                                        <div class="col-12">
                                            <label class="form-label">Ghi chú</label>
                                            <textarea name="GhiChu" class="form-control" rows="2"
                                                      placeholder="Tiền sử bệnh, dị ứng..."></textarea>
                                        </div>
                                    </div>
                                </div>

                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                                    <button type="submit" class="btn btn-primary">
                                        <i class="fas fa-save me-2"></i>Lưu hồ sơ
                                    </button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
                <!-- Edit Profile Modal -->
                <div class="modal fade" id="editProfileModal" tabindex="-1">
                    <div class="modal-dialog modal-lg">
                        <div class="modal-content">
                            <form method="post" action="@Url.Action("ChinhSuaHoSo", "HoSo")">
                                @Html.AntiForgeryToken()
                                <div class="modal-header">
                                    <h5 class="modal-title">
                                        <i class="fas fa-edit me-2"></i>Chỉnh sửa hồ sơ tiêm chủng
                                    </h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                </div>
                                <div class="modal-body">
                                    <input type="hidden" name="MaHSTC" id="editMaHSTC" />
                                    <div class="row">
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label">Họ và tên</label>
                                            <input type="text" name="HoTen" id="editHoTen" class="form-control" required />
                                        </div>
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label">Giới tính</label>
                                            <select name="GioiTinh" id="editGioiTinh" class="form-select" required>
                                                <option>Nam</option>
                                                <option>Nữ</option>
                                            </select>
                                        </div>
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label">Ngày sinh</label>
                                            <input type="date" name="NgaySinh" id="editNgaySinh" class="form-control" required />
                                        </div>
                                        <div class="col-md-6 mb-3">
                                            <label class="form-label">CCCD</label>
                                            <input type="text" name="CCCD" id="editCCCD" class="form-control" />
                                        </div>
                                        <div class="col-12 mb-3">
                                            <label class="form-label">Ghi chú</label>
                                            <textarea name="GhiChu" id="editGhiChu" class="form-control" rows="2"></textarea>
                                        </div>
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                                    <button type="submit" class="btn btn-primary">Lưu thay đổi</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
                @*JavaScript để đổ dữ liệu vào modal*@
                <script>
                    function loadHoSoVaoModal(ma, ten, gioiTinh, ngaySinh, cccd, ghiChu) {
                        document.getElementById("editMaHSTC").value = ma;
                        document.getElementById("editHoTen").value = ten;
                        document.getElementById("editGioiTinh").value = gioiTinh;
                        document.getElementById("editNgaySinh").value = ngaySinh;
                        document.getElementById("editCCCD").value = cccd;
                        document.getElementById("editGhiChu").value = ghiChu;
                    }
                </script>



                <script src="Scripts/bootstrap.bundle.min.js"></script>
                <script>
      // Section Navigation
      const menuItems = document.querySelectorAll(".menu-item");
      const sections = document.querySelectorAll(".section-content");

      function switchSection(sectionName) {
        // Hide all sections
        sections.forEach((section) => {
          section.style.display = "none";
        });

        // Remove active class from all menu items
        menuItems.forEach((item) => {
          item.classList.remove("active");
        });

        // Show selected section
        const targetSection = document.getElementById(`section-${sectionName}`);
        if (targetSection) {
          targetSection.style.display = "block";
        }

        // Add active class to clicked menu item
        const targetMenuItem = document.querySelector(
          `[data-section="${sectionName}"]`
        );
        if (targetMenuItem) {
          targetMenuItem.classList.add("active");
        }

        // Update URL hash
        window.location.hash = sectionName;
      }

      // Menu click handlers
      menuItems.forEach((item) => {
        item.addEventListener("click", function (e) {
          const sectionName = this.getAttribute("data-section");
          if (sectionName) {
            e.preventDefault();
            switchSection(sectionName);
          }
        });
      });

      // Handle initial hash on page load
      window.addEventListener("DOMContentLoaded", function () {
        const hash = window.location.hash.substring(1);
        if (hash) {
          switchSection(hash);
        }
      });

      console.log("Dashboard loaded successfully");

      // Logout function
      function handleLogout(e) {
        e.preventDefault();
        sessionStorage.removeItem("isLoggedIn");
        sessionStorage.removeItem("userName");
        alert("✓ Đăng xuất thành công!");
        window.location.href = "@Url.Action("Index", "Home")";
      }

      // Check login state - Commented out since backend doesn't exist yet
      // const isLoggedIn = sessionStorage.getItem("isLoggedIn") === "true";
      // if (!isLoggedIn) {
      //   alert("⚠️ Vui lòng đăng nhập để truy cập dashboard!");
      //   window.location.href = "@Url.Action("Login", "Account")";
    // }

                </script>

                @section Scripts {
                    <!-- Import jQuery -->
                    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

                    <script>
        // Handle initial hash on page load
        window.addEventListener("DOMContentLoaded", function () {
            const hash = window.location.hash.substring(1);
            if (hash) {
                switchSection(hash);
            }
        });

        console.log("Dashboard loaded successfully");

        // Logout function
        function handleLogout(e) {
            e.preventDefault();
            sessionStorage.removeItem("isLoggedIn");
            sessionStorage.removeItem("userName");
            alert("✓ Đăng xuất thành công!");
            window.location.href = "@Url.Action("Index", "Home")";
        }

        // Hiển thị date picker (cho nút đổi lịch cũ)
        function showDatePicker(maLT) {
            openDoiLichModal(maLT);
        }

        // Đổi lịch với SweetAlert2 (cho nút đổi lịch cũ)
        function updateSchedule(maLT) {
            openDoiLichModal(maLT);
        }

        // Mở modal đổi lịch với giao diện đẹp
        function openDoiLichModal(maLT) {
            // Tính ngày tối thiểu (ngày mai)
            var tomorrow = new Date();
            tomorrow.setDate(tomorrow.getDate() + 1);
            var minDate = tomorrow.toISOString().split('T')[0];
            
            // Tính ngày tối đa (1 năm sau)
            var maxDate = new Date();
            maxDate.setFullYear(maxDate.getFullYear() + 1);
            var maxDateStr = maxDate.toISOString().split('T')[0];

            Swal.fire({
                title: '<i class="fas fa-calendar-alt text-primary"></i> Đổi lịch hẹn',
                html: `
                    <div class="text-start">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Chọn ngày mới:</label>
                            <input type="date" id="newDateInput" class="form-control" 
                                   min="${minDate}" max="${maxDateStr}" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label fw-bold">Chọn khung giờ:</label>
                            <select id="newTimeInput" class="form-select">
                                <option value="08:00">08:00 - Sáng sớm</option>
                                <option value="09:00">09:00 - Sáng</option>
                                <option value="10:00" selected>10:00 - Sáng</option>
                                <option value="11:00">11:00 - Trưa</option>
                                <option value="14:00">14:00 - Chiều</option>
                                <option value="15:00">15:00 - Chiều</option>
                                <option value="16:00">16:00 - Chiều</option>
                            </select>
                        </div>
                        <div class="alert alert-info small mb-0">
                            <i class="fas fa-info-circle me-1"></i>
                            Lưu ý: Bạn chỉ có thể đổi lịch trước ngày hẹn ít nhất 1 ngày.
                        </div>
                    </div>
                `,
                showCancelButton: true,
                confirmButtonText: '<i class="fas fa-check me-1"></i>Xác nhận đổi lịch',
                cancelButtonText: '<i class="fas fa-times me-1"></i>Hủy bỏ',
                confirmButtonColor: '#0077b6',
                cancelButtonColor: '#6c757d',
                width: '450px',
                preConfirm: () => {
                    const newDate = document.getElementById('newDateInput').value;
                    const newTime = document.getElementById('newTimeInput').value;
                    
                    if (!newDate) {
                        Swal.showValidationMessage('Vui lòng chọn ngày mới!');
                        return false;
                    }
                    
                    // Kiểm tra ngày không phải Chủ nhật
                    var selectedDate = new Date(newDate);
                    if (selectedDate.getDay() === 0) {
                        Swal.showValidationMessage('Trung tâm không làm việc vào Chủ nhật. Vui lòng chọn ngày khác!');
                        return false;
                    }
                    
                    return newDate + 'T' + newTime;
                }
            }).then((result) => {
                if (result.isConfirmed) {
                    // Hiển thị loading
                    Swal.fire({
                        title: 'Đang xử lý...',
                        html: 'Vui lòng đợi trong giây lát',
                        allowOutsideClick: false,
                        didOpen: () => {
                            Swal.showLoading();
                        }
                    });

                    $.ajax({
                        url: '@Url.Action("DoiLichNgay", "LichTiem")',
                        type: 'POST',
                        data: { 
                            MaLT: maLT, 
                            NgayHenTiem: result.value,
                            __RequestVerificationToken: $('input[name="__RequestVerificationToken"]').val()
                        },
                        success: function (response) {
                            if (response.success) {
                                Swal.fire({
                                    icon: 'success',
                                    title: 'Đổi lịch thành công!',
                                    html: `<p>${response.message}</p>
                                           <p class="text-muted small">Trang sẽ tự động tải lại...</p>`,
                                    confirmButtonColor: '#0077b6',
                                    timer: 2000,
                                    timerProgressBar: true
                                }).then(() => location.reload());
                            } else {
                                Swal.fire({
                                    icon: 'error',
                                    title: 'Không thể đổi lịch!',
                                    text: response.message,
                                    confirmButtonColor: '#dc3545'
                                });
                            }
                        },
                        error: function (xhr) {
                            var errorMsg = 'Không thể kết nối đến server.';
                            if (xhr.responseJSON && xhr.responseJSON.message) {
                                errorMsg = xhr.responseJSON.message;
                            }
                            Swal.fire({
                                icon: 'error',
                                title: 'Lỗi!',
                                text: errorMsg,
                                confirmButtonColor: '#dc3545'
                            });
                        }
                    });
                }
            });
        }

        // Hủy lịch với SweetAlert2 - CHỨC NĂNG ĐÃ TẠM DỪNG
        function cancelSchedule(maLT) {
            Swal.fire({
                icon: 'info',
                title: '<i class="fas fa-info-circle text-info"></i> Thông báo',
                html: `
                    <div class="text-start">
                        <p class="mb-3">Chức năng hủy lịch trực tuyến hiện đang tạm dừng.</p>
                        <div class="alert alert-info small mb-0">
                            <i class="fas fa-phone me-1"></i>
                            Vui lòng liên hệ trung tâm TPVAX qua hotline <strong>1900-xxxx</strong> để được hỗ trợ hủy lịch.
                        </div>
                    </div>
                `,
                confirmButtonText: '<i class="fas fa-check me-1"></i>Đã hiểu',
                confirmButtonColor: '#0077b6',
                width: '450px'
            });
        }
        // Copy voucher code
        function copyVoucherCode(code) {
            navigator.clipboard.writeText(code).then(function() {
                Swal.fire({
                    icon: 'success',
                    title: 'Đã sao chép!',
                    text: 'Mã khuyến mãi: ' + code,
                    timer: 2000,
                    showConfirmButton: false
                });
            }).catch(function() {
                // Fallback for older browsers
                var textArea = document.createElement("textarea");
                textArea.value = code;
                document.body.appendChild(textArea);
                textArea.select();
                document.execCommand('copy');
                document.body.removeChild(textArea);
                
                Swal.fire({
                    icon: 'success',
                    title: 'Đã sao chép!',
                    text: 'Mã khuyến mãi: ' + code,
                    timer: 2000,
                    showConfirmButton: false
                });
            });
        }

        document.addEventListener("DOMContentLoaded", function () {
            const activeTab = document.querySelector(".nav-link.active");
            if (activeTab) {
                const target = activeTab.getAttribute("data-bs-target");
                const tabPane = document.querySelector(target);
                if (tabPane) {
                    tabPane.classList.add("show", "active");
                }
            }

            // Debug form submit
            const formThemHoSo = document.getElementById('formThemHoSo');
            if (formThemHoSo) {
                formThemHoSo.addEventListener('submit', function(e) {
                    console.log('Form submitting to:', this.action);
                    console.log('Method:', this.method);
                    console.log('Form data:');
                    const formData = new FormData(this);
                    for (let [key, value] of formData.entries()) {
                        console.log(key + ':', value);
                    }
                });
            }

            // AJAX cho form cập nhật thông tin
            $('form[action*="CapNhatThongTin"]').on('submit', function(e) {
                e.preventDefault();
                var form = $(this);
                $.ajax({
                    url: form.attr('action'),
                    type: 'POST',
                    data: form.serialize(),
                    success: function(response) {
                        if (response.success) {
                            Swal.fire({
                                icon: 'success',
                                title: 'Thành công!',
                                text: response.message,
                                confirmButtonColor: '#0077b6'
                            });
                        } else {
                            Swal.fire({
                                icon: 'error',
                                title: 'Lỗi!',
                                text: response.message,
                                confirmButtonColor: '#dc3545'
                            });
                        }
                    },
                    error: function() {
                        Swal.fire({
                            icon: 'error',
                            title: 'Lỗi!',
                            text: 'Không thể kết nối đến server.',
                            confirmButtonColor: '#dc3545'
                        });
                    }
                });
            });

            // AJAX cho form đổi mật khẩu
            $('form[action*="DoiMatKhau"]').on('submit', function(e) {
                e.preventDefault();
                var form = $(this);
                $.ajax({
                    url: form.attr('action'),
                    type: 'POST',
                    data: form.serialize(),
                    success: function(response) {
                        if (response.success) {
                            Swal.fire({
                                icon: 'success',
                                title: 'Thành công!',
                                text: response.message,
                                confirmButtonColor: '#0077b6'
                            });
                            form[0].reset();
                        } else {
                            Swal.fire({
                                icon: 'error',
                                title: 'Lỗi!',
                                text: response.message,
                                confirmButtonColor: '#dc3545'
                            });
                        }
                    },
                    error: function() {
                        Swal.fire({
                            icon: 'error',
                            title: 'Lỗi!',
                            text: 'Không thể kết nối đến server.',
                            confirmButtonColor: '#dc3545'
                        });
                    }
                });
            });

            // Lọc lịch sử tiêm chủng theo hồ sơ được chọn
            $('#selectHoSo').on('change', function() {
                var selectedMaHSTC = $(this).val();
                $('.timeline-item').each(function() {
                    var maHSTC = $(this).data('hoso');
                    if (maHSTC == selectedMaHSTC || !selectedMaHSTC) {
                        $(this).show();
                    } else {
                        $(this).hide();
                    }
                });
            });
            // Trigger filter on load
            $('#selectHoSo').trigger('change');
        });

        // Xuất PDF sổ tiêm chủng
        function xuatPDFSoTiemChung() {
            var maHSTC = $('#selectHoSo').val();
            if (!maHSTC) {
                Swal.fire({
                    icon: 'warning',
                    title: 'Chưa chọn hồ sơ',
                    text: 'Vui lòng chọn hồ sơ tiêm chủng để xuất PDF.',
                    confirmButtonColor: '#0077b6'
                });
                return;
            }
            window.location.href = '@Url.Action("XuatPDFSoTiemChung", "HoSo")' + '?maHSTC=' + maHSTC;
        }

                    </script>
                }

