@model IEnumerable<TPVAXWebsite.Models.ViewModels.VaccineRecommendationViewModel>
@{
    ViewBag.Title = "Tôi Nên Tiêm Gì?";
}

<main>
    <!-- Hero -->
    <section class="advice-consult-hero text-start">
        <div class="container position-relative">
            <img src="@Url.Content("~/Content/Assets/Images/tntg.jpg")"
                 alt="Bác sĩ tư vấn"
                 class="hero-image d-none d-lg-block" />
            <div class="row">
                <div class="col-lg-6 py-5">
                    <nav aria-label="breadcrumb" class="mb-1">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item">
                                <a href="@Url.Action("Index","Home")" class="text-secondary">Trang chủ</a>
                            </li>
                            <li class="breadcrumb-item active text-primary" aria-current="page">
                                Tôi nên tiêm gì?
                            </li>
                        </ol>
                    </nav>
                    <h1 class="fw-bold hero-title">Tôi nên tiêm gì?</h1>
                    <p class="lead hero-subtitle">
                        Chọn thông tin và nhận đề xuất các vắc xin cần tiêm
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Filter Form -->
    <section class="container filter-block mt-4">
        <form method="get" class="row g-3 align-items-end">
            <div class="col-md-5">
                <label for="ageSelect" class="form-label">Độ tuổi</label>
                <select class="form-select" id="ageSelect" name="ageGroup">
                    <option value="">-- Chọn độ tuổi --</option>
                    <option value="6-12 tháng" @(ViewBag.AgeGroup == "6-12 tháng" ? "selected" : "")>6-12 tháng</option>
                    <option value="1-5 tuổi" @(ViewBag.AgeGroup == "1-5 tuổi" ? "selected" : "")>1-5 tuổi</option>
                    <option value="Người lớn" @(ViewBag.AgeGroup == "Người lớn" ? "selected" : "")>Người lớn</option>
                </select>
            </div>

            <div class="col-md-4">
                <label class="form-label d-block">Chọn giới tính</label>
                <select class="form-select" name="gender">
                    <option value="">-- Giới tính --</option>
                    <option value="Nam" @(ViewBag.Gender == "Nam" ? "selected" : "")>Nam</option>
                    <option value="Nữ" @(ViewBag.Gender == "Nữ" ? "selected" : "")>Nữ</option>
                </select>
            </div>

            <div class="col-md-3">
                <button type="submit" class="btn btn-primary w-100">Nhận kết quả</button>
            </div>
        </form>
    </section>

    <!-- Results -->
    @if (Model != null && Model.Any())
    {
        <section class="advice-section row g-4 mt-5">
            <div class="col-12">
                <h2 class="section-title fw-bold">
                    <i class="fas fa-star section-title-icon me-2"></i>
                    Đề xuất cho: @ViewBag.AgeGroup - @ViewBag.Gender
                </h2>
            </div>

            @foreach (var vaccine in Model)
            {
                <div class="col-lg-4">
                    <div class="card h-100">
                        <img src="@Url.Content("~/Content/Images/" + vaccine.HinhAnh)"
                             class="card-img-top" alt="@vaccine.TenVC" />
                        <div class="card-body">
                            <h5 class="card-title">@vaccine.TenVC</h5>
                            <p class="card-text">@vaccine.MoTa</p>
                            <p class="fw-bold text-danger">@vaccine.GiaBan.ToString("N0") VNĐ</p>
                            <a href="@Url.Action("Detail", "ToiNenTiemGi", new { id = vaccine.MaVC })"
                               class="btn btn-outline-primary">Chi tiết</a>
                        </div>
                    </div>
                </div>
            }
        </section>
    }
    else if (ViewBag.AgeGroup != null || ViewBag.Gender != null)
    {
        <p class="mt-4">Không tìm thấy vaccine phù hợp với lựa chọn của bạn.</p>
    }
</main>
